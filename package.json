{
  "name": "blog-man-api",
  "type": "module",
  "main": "handler.js",
  "version": "1.0.0",
  "description": "AWS Lambda functions for managing blogs with authentication.",
  "scripts": {
    "dev": "nodemon --exec serverless offline",
    "deploy": "serverless deploy",
    "remove": "serverless remove"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "prisma": "^4.10.1",
    "serverless-dotenv-plugin": "^4.0.2",
    "serverless-offline": "^12.0.4"
  },
  "dependencies": {
    "@prisma/client": "^4.10.1",
    "jsonwebtoken": "^9.0.0"
  }
}
